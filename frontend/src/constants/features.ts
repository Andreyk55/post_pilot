/**
 * Feature configuration constants.
 * These should match the backend Features configuration.
 */

/**
 * Maximum number of platforms that can be selected per post.
 * When set to 1, the UI renders radio buttons for single selection.
 * When > 1, the UI allows multi-select (checkboxes).
 */
export const MAX_PLATFORMS_PER_POST = 1
